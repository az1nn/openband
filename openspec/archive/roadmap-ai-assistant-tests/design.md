# AI Audio Assistant Test Suite — Design

## 1. Test Coverage (`tests/aiAssistant.test.ts`)
- Test intent classification for prompts like "add a chord progression in C minor" or "set bpm to 128".
- Test voice command mapping to command registry actions.
- Test fallback responses when AI service is unavailable.
