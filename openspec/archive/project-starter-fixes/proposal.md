# Proposal: project-starter-fixes (empty stub — superseded)

This folder was an empty stub (no `proposal.md`/`design.md`/`tasks.md`) left in
`openspec/changes/`. The project-starter work it was meant to track is covered by the
archived `project-starter-wiring` change and the canonical
`openspec/specs/project-starter/spec.md`. Archived as stale per the docs-reconciliation
sweep; no action was ever taken against it.
