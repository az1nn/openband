# Tasks: Studio Edit Freeze Fix

- [ ] Create spec files (`proposal.md`, `design.md`, `tasks.md`) under `openspec/changes/studio-edit-freeze-fix/`
- [ ] Commit spec files
- [ ] Implement cache key optimization in `app/studio/hooks.ts` (`renderTracksCached`)
- [ ] Run `npx tsc --noEmit` and `npx vitest run` to verify correctness
- [ ] Leave implementation uncommitted
