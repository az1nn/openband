# Tasks: Add Fourth Round of Unit Tests and Validation

- [ ] Add `listProjectIndex` persistence test for `parentProjectId`
- [ ] Expand pure-JS WAV decoding test cases for 8-bit, 24-bit, and 32-bit float PCM in `tests/audioExport.test.ts`
- [ ] Add edge-case test cases for `wouldCreateCycle` with missing source and target nodes in `tests/lib3.test.ts`
- [ ] Run `npx vitest run` to verify all tests pass successfully
