# Next Step D: Hardware MIDI Controller & MCU Support — Tasks

- [ ] Implement `src/lib/mcuController.ts` MCU protocol mapper.
- [ ] Add unit tests in `tests/mcuController.test.ts`.
- [ ] Run verification (`tsc`, `vitest`, `graph:ci`).
