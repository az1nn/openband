# Prioritized Roadmap Focus: Automation, Hardware I/O, and CRDT Sync — Design

## 1. Enhancements
- **Automation (`automationEngine.ts`)**: Add sample-accurate parameter smoothing helpers (`setValueCurveAtTime`) and bezier curve interpolation support for automation points.
- **Hardware I/O (`hardwareIO.ts`)**: Add buffer size and latency calibration methods to the `NativeBridge` interface and browser implementation.
- **CRDT (`crdt.ts`)**: Add operation history compaction / pruning for vector clocks.
- **Future Roadmap Docs (`docs/future-roadmap.html`)**: Interactive documentation page covering Stem Separation (Focus 1) and 3D Studio LOD/Frustum Culling (Focus 4).
