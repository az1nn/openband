# Roadmap Step 4: Native Desktop Build Pipeline & CI Automation — Tasks

- [ ] Create `.github/workflows/ci.yml` automation pipeline.
- [ ] Configure `electron/package.json` build targets.
- [ ] Add `tests/electronPreloadSecurity.test.ts` verifying contextBridge method isolation.
- [ ] Run verification checks (`tsc`, `vitest`, `graph:ci`).
