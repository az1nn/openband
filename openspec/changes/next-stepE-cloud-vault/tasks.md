# Next Step E: Cloud Project Vault & S3 Asset Deduplication — Tasks

- [ ] Implement SHA-256 asset hashing and deduplication check in `src/lib/supabaseRemote.ts`.
- [ ] Add unit tests in `tests/cloudVault.test.ts`.
- [ ] Run verification (`tsc`, `vitest`, `graph:ci`).
