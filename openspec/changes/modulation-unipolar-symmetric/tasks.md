# Tasks: Symmetric Unipolar Modulation

- [ ] 1. Update `computeModulation` in `src/lib/modulationMatrix.ts` to map non-bipolar sources symmetrically (`[-amount, +amount]` centered).
- [ ] 2. Verify TypeScript compilation (`node ./node_modules/typescript/bin/tsc --noEmit`).
- [ ] 3. Run modulation tests (`tests/modulation.test.ts`, `tests/modulationMatrix.test.ts`, `tests/modulationMatrixRender.test.ts`).
- [ ] 4. Ensure all checks pass and changes remain uncommitted per instructions.
