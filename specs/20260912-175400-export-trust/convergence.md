# Convergence Boundary

Implementation convergence must stay inside the approved #49 surfaces and contract.

If implementation evidence shows that trustworthy WAV export requires changing PlaybackEngine architecture, persistent project formats, native bridge contracts, or deterministic DSP/plugin/codec algorithms, stop and return to design analysis rather than absorbing that work into this feature.
