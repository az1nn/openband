# Human Design Gate

Status: **READY FOR HUMAN APPROVAL after design verification passes**.

The approval must bind to the exact final design HEAD and authorizes implementation only inside the design envelope described by `spec.md`, `plan.md` and `docs/contracts/audio-export.md`.

Approval becomes stale if the design materially changes renderer ownership, mixer-unit semantics, strict failure behavior, format guarantees, verification strategy, or crosses into correctness-critical DSP/codec algorithm changes.
