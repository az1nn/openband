# Durable Contracts

Place a contract here only when future features must repeatedly rely on the same boundary or invariant, such as a public API, cross-runtime bridge, persistent data format, or important cross-feature domain rule.

Feature-local interfaces belong in that feature's Spec Kit artifacts. UI minutiae and implementation details do not belong here.

Contracts describe current truth and must be reconciled by any change that invalidates them.
