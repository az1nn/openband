# OpenBand Session Router

Canonical project-scoped skill: `SKILL.md`.

This directory owns the OpenBand-specific `siga` routing contract. The historical `.qwen/skills/auto-skill-session-router/SKILL.md` path is a compatibility adapter and must delegate here.

Canonical repository: `az1nn/openband`.
